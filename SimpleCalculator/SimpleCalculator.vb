Imports System.IO

Module SimpleCalculator

    Sub Main()
        Dim JellyBeans As Integer
        Dim FootBall As Integer
        Console.WriteLine(" Input numbers and + or - ")
        JellyBeans = Console.Read
        Console.ReadLine()
        FootBall = Console.Read
        Console.WriteLine(JellyBeans + FootBall)
    End Sub

End Module
